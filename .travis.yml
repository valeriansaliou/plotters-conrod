language: rust
rust:
  - stable

jobs:
  before_install:
    - sudo apt-get update -y
    - sudo apt-get install -y build-essential g++ libxcb-shape0 libxcb-shape0-dev libxcb-xfixes0 libxcb-xfixes0-dev

cache: cargo
